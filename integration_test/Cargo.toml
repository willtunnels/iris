[package]
name = "integration_test"
version = "0.1.0"
edition = "2018"

[build-dependencies]
iris = { path = ".." }

[dependencies]
iris = { path = ".." }
serde = { version = "1.0", features = ["derive"] }
